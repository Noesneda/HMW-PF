function laCajaDePandora(numero){
   if (numero % 2 === 0) {
      let n = numero;
      let b = (numero % 2).toString();
   while(numero > 0) {
     n = (numero / 2);
     b = (num % 2) + b; 
    }
     return b;
   } else {
      let hexadecimal = "de lo contrario hexadecimal"
      return hexadecimal;
   } 
    }




   let Alejandra = () => {
      let obj = {
         nombre: "Alejandra",
         edad: 30,
         nacionalidad: "Colombiana"
      }
      return obj;
   }

    